# 轻量迭代：Markdown 可点击文件名样式修复

## 目标
- 行内代码中的可打开文件：仅显示文件名（浅蓝背景/圆角、无描边），点击打开；悬浮提示保留完整路径
- 非文件行内代码：保持浅底圆角（无描边）
- 视觉对齐：与行内普通文字基线更一致，避免 UI 容器导致的偏移

## 任务清单
- [√] 行内代码文件引用渲染为可点击元素（无描边）
- [√] 点击时重试解析并打开文件/目录
- [-] 悬停手型光标（WinUI `ProtectedCursor` 为受保护成员，暂不启用）
- [√] 迁移方案包至 history 并更新索引
